# Progress Log

- Started refactor wave 1 planning.
- Saved persistent plan/findings/progress files.
- Scope chosen: helper deduplication and boundary cleanup only.
- Baseline verification passed: focused tests (275 pass), typecheck pass, build pass.
- Parallel implementation launched for two independent slices: governance helper extraction and config/provider/stuck helper dedupe.
- Shared governance helper extraction completed and rewired in runtime/tool call sites.
- Shared config path resolver, provider helper reuse, and shared stuck predicate completed.
- Post-change diagnostics clean on modified files (existing unused-symbol hints remain in unrelated pre-existing areas).
- Focused tests passed (164 + 18 + 51), post-change typecheck/build passed, and full suite passed (709 tests).
- Manual QA passed for readiness config resolution and shared stuck predicate behavior.
- Started wave 2 analysis for metrics/event sink unification.
- Determined minimal safe slice: shared JSONL append helper plus index-core metrics migration from `metrics.json` array writes to `metrics.jsonl` append writes, while preserving legacy read fallback.
- Wave 2 plan saved and baseline checks passed (224 focused tests, typecheck, build).
- Shared JSONL sink implemented and wired into index-core metrics/latency/route logging plus control-tools metrics append.
- Wave 2 focused tests, full test suite, typecheck, and build all passed.
- Manual QA passed: real plugin hook flow created `.Aegis/metrics.jsonl` and produced readable JSON records.
- Started wave 3 analysis for OpenCode client compatibility helper dedupe.
- Chosen minimal slice: shared helper for `session.promptAsync` and `config.providers`, scoped to `src/index-core.ts` and `src/tools/control-tools.ts` only.
- Wave 3 plan saved and baseline checks passed (118 focused tests, typecheck, build).
- Shared OpenCode client compat helper implemented and wired into index-core plus control-tools.
- Wave 3 focused tests, full test suite, typecheck, and build all passed.
- Manual QA passed: provider lookup and synthetic prompt injection both executed successfully through the real plugin.
- Started wave 4 analysis for session-store reducer extraction.
- Wave 4 plan saved and baseline checks passed (150 focused tests, typecheck, build).
- Shared session event reducer implemented and wired into `SessionStore.applyEvent`.
- Wave 4 focused tests, full test suite, typecheck, and build all passed.
- Manual QA passed: real `SessionStore` transitions still moved to `SUBMIT` with `candidateLevel=L2` after verify success.
- Started wave 5 analysis for route logging extraction.
- Wave 5 plan saved and baseline checks passed (118 focused tests, typecheck, build).
- Shared route logging helper implemented and wired into `index-core`.
- Wave 5 focused tests, full test suite, typecheck, and build all passed.
- Manual QA passed: real plugin execution produced `.Aegis/route_decisions.jsonl` with readable `RouteDecision` output.
- Started wave 6 analysis for autoloop extraction.
- Wave 6 helper was introduced under `src/orchestration/auto-loop.ts` and wired back into `index-core` after a declaration-order fix.
- Wave 6 focused tests, full test suite, typecheck, and build all passed.
- Manual QA passed: idle-event autoloop still attempted envelope prompt first and then fallback synthetic prompt dispatch.
- Started wave 7 analysis for startup toast extraction.
- Wave 7 plan saved and baseline showed one unrelated `plugin-hooks` timeout, so startup-toast-specific regressions were judged against the focused behavior plus full suite.
- Shared startup toast helper implemented and wired into `index-core`; fallback semantics were corrected after the first extraction pass.
- Wave 7 typecheck/build passed, full suite passed, and manual QA confirmed a single `oh-my-Aegis` startup toast emission.
